#!/usr/bin/env python3
"""kdiff — compare Kubernetes resources between two clusters

Lightweight wrapper that imports and runs the main CLI module.
"""
import sys
from pathlib import Path

# Add parent directory to path to import kdiff_cli
ROOT = Path(__file__).resolve().parent.parent
sys.path.insert(0, str(ROOT))

# Import and run main CLI
from kdiff_cli import main

if __name__ == '__main__':
    sys.exit(main())
